from flask import render_templatefrom flask.views import MethodViewimport gbmodelclass ViewSongs(MethodView):    def get(self):        model = gbmodel.get_model()                # Fetch songs directly, as get_all_songs already provides a list of song dictionaries        songs = model.get_all_songs()        return render_template('view.html', songs=songs)